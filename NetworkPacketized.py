def CreatePacket(crc, length, data, flag):
	


if __name__ =="__main__"
	crc, length, data, flag = input("ENter crc, length, data, flag : ")
	packet = CreatePacket(crc, length, data, flag)
	print packet